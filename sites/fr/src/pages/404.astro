---
import BaseLayout from '@mc/shared/layouts/BaseLayout.astro';
import { locale, nav, footer } from '../chrome.ts';
import bodyHtml from '../../content/404.html?raw';

const title = "Page introuvable | Monsieur Click";
const description = "Cette page n";
const canonical = "";
const hreflang = [];
const ogImage = undefined;
const jsonLd = [];
const scripts = ["/shared.js?v=20260717ds2","/perf.js?v=20260717ds2"];
---
<BaseLayout
  locale={locale}
  title={title}
  description={description}
  canonical={canonical}
  hreflang={hreflang}
  ogImage={ogImage}
  jsonLd={jsonLd}
  scripts={scripts}
  nav={nav}
  footer={footer}
>
  <Fragment set:html={bodyHtml} />
</BaseLayout>
